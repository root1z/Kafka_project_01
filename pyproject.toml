[project]
name = "kafka-project-01"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "colorlog>=6.10.1",
    "confluent-kafka>=2.12.1",
    "pymongo>=4.15.3",
    "python-dotenv>=1.0.1",
]
